{% extends 'base.html.twig' %}

{% block title %}Hello AdministradorController!{% endblock %}

{% block stylesheets %}
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,400;0,500;0,700;1,500;1,700&display=swap" rel="stylesheet">
    <style>
        body {
            background-image: url('{{ asset('assets/FondoPantallaAdministrador.jpg') }}');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        .font-\[inter\] { font-family: 'Inter', sans-serif; }
    </style>
{% endblock %}

{% block body %}
    <div class="flex justify-center items-center min-h-screen">
        <div class="bg-white rounded-lg shadow-lg p-8 w-full max-w-md">
            <h2 class="text-2xl font-bold text-gray-700 text-center mb-8">Pantalla de Administrador</h2>
            <div class="flex flex-col gap-6 items-center">
                <a href="{{ path('gestion_usuarios') }}"
                   class="w-64 py-2 rounded-lg text-white text-lg font-semibold text-center bg-blue-500 hover:bg-blue-600 transition shadow">
                    Gestión de Usuarios
                </a>
                <a href="{{ path('gestion_recetas') }}"
                   class="w-64 py-2 rounded-lg text-white text-lg font-semibold text-center bg-gray-500 hover:bg-gray-600 transition shadow">
                    Gestión de Recetas
                </a>
                <a href="{{ path('gestion_ingredientes') }}"
                   class="w-64 py-2 rounded-lg text-white text-lg font-semibold text-center bg-green-600 hover:bg-green-700 transition shadow">
                    Gestión de Ingredientes
                </a>
            </div>
            <form method="post" action="{{ path('app_logout') }}">
                <button type="submit"
                        class="mt-8 bg-red-500 hover:bg-red-600 text-white font-semibold py-2 px-6 rounded shadow transition">
                    Cerrar sesión
                </button>
            </form>
        </div>
    </div>
{% endblock %}
